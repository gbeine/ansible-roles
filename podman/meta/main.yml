---

dependencies:

  - name: Import role net_common
    role: net_common

  - name: Import role bootstrap_timezone
    role: bootstrap_timezone

  - name: Import role bootstrap_accounts
    role: bootstrap_accounts
    vars:
      bootstrap_users: "{{ podman__users }}"
      bootstrap_groups: "{{ podman__users }}"

  - name: Import role pki_easyrsa_deploy
    role: pki_easyrsa_deploy
