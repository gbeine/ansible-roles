---
#
# Do not edit this file.
# Replace variables using substitutions described
# https://ansible-roles.org/roles/bootstrap-accounts
#

bootstrap_accounts__users: "{{ bootstrap_users | default([]) }}"
bootstrap_accounts__groups: "{{ bootstrap_groups | default([]) }}"
