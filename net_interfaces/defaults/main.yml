---

#
# Substitutes - do not change
#

net_interfaces__interfaces: "{{ network_interfaces | default([]) }}"

#
# Defaults - do not change
#

net_interfaces__etc_dir: /etc/network
net_interfaces__interfaces_dir: "{{ net_interfaces__etc_dir }}/interfaces.d"
net_interfaces__interfaces_file: "{{ net_interfaces__etc_dir }}/interfaces"
