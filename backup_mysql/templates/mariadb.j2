###
### {{ ansible_managed }}
###

daily_backup_mariadb_enable="{{ backup_mysql__use_podman | ternary('YES', 'NO') }}"
daily_backup_mariadb_compress="{{ backup_mysql__compress | ternary('YES','NO') }}"
daily_backup_mariadb_replication="{{ backup_mysql__replication | ternary('YES','NO') }}"
daily_backup_mariadb_backup_dir="{{ backup_mysql__backup_dir }}"
daily_backup_mariadb_host="{{ backup_mysql__host }}"
daily_backup_mariadb_user="{{ backup_mysql__user }}"
daily_backup_mariadb_password="{{ backup_mysql__password }}"
daily_backup_mariadb_dump_options="{{ backup_mysql__dump_options }}"
daily_backup_mariadb_ignore_databases="{{ backup_mysql__ignore_databases | join(' ') }}"
daily_backup_mariadb_keep_days="-{{ backup_mysql__keep_days }}days"
daily_backup_mariadb_podman_container="mariadb"
