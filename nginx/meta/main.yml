---

dependencies:

- name: Setup PKI
  tags: [ 'nginx', 'pki' ]
  when: nginx__enable and (nginx__acme_enable or nginx__pki_enable)
  role: pki

- name: Setup PHP
  tags: [ 'nginx', 'php' ]
  when: nginx__enable and nginx__php_enable
  role: php
