---

nginx__acme_enable: "{{ nginx_acme_enable | default(false) }}" 
nginx__acme_root: "{{ nginx_acme_root | default('/var/www/acme') }}"
nginx__acme_domain: "{{ nginx_acme_domain | default('acme') }}"

#
# Do not change from here
#

nginx__acme_webserver_options: "--nginx"

nginx__acme_environment: {}

nginx__acme_packages:
- python3-certbot-nginx
