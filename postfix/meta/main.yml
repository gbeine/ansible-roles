---

dependencies:

- name: Setup clamav
  tags: [ 'postfix', 'clamav' ]
  when: postfix__enable and clamav__enable
  role: clamav

- name: Setup MySQL client
  tags: [ 'postfix', 'mysql' ]
  when: postfix__enable and postfix__dmarc_enable
  role: mysql
  vars:
    mysql_client_enable: true
