###
### {{ ansible_managed }}
###

[Timer]
# Empty assignment resets the list first; without it this schedule is ADDED to
# the one from container-update@.timer and the timer fires on both.
OnCalendar=
OnCalendar={{ container_update__schedule }}
