---

#
# Substitutes - do not change
#

backup_postgresql__backup_dir: "{{ backup_postgresql_backup_dir | default('/var/backups/mysql') }}"
backup_postgresql__ignore_databases: "{{ backup_postgresql_ignore_databases | default(backup_postgresql__ignore_databases_default) }}"
backup_postgresql__keep_days: "{{ backup_postgresql_keep_days | default('14') }}"
backup_postgresql__podman_backup_dir: "{{ backup_postgresql_backup_dir | default('/backup') }}"

#
# Defaults - do not change
#

backup_postgresql__ignore_databases_default:
  - 'template0'
  - 'template1'

backup_postgresql__files:
  - '910.backup.postgresql'
