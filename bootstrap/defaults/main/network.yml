---

bootstrap__network_enable: "{{ bootstrap_network_enable | default(false) }}"
bootstrap__network_interfaces: "{{ bootstrap_network_interfaces | default({}) }}"
bootstrap__network_bind_ssh: "{{ bootstrap_network_bind_ssh | default('') }}"

