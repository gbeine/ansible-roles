---

bootstrap__resolv_domain: "{{ bootstrap_resolv_domain | default('localhost') }}"
bootstrap__resolv_search: "{{ bootstrap_resolv_search | default(['localdomain']) }}"
bootstrap__resolv_nameserver: "{{ bootstrap_resolv_nameserver | default([]) }}"
