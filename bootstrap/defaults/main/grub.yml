---

bootstrap__grub_enable: "{{ bootstrap_grub_enable | default(false) }}"
bootstrap__grub_cmdline_linux_default: "{{ bootstrap_grub_cmdline_linux_default | default('quiet') }}"
bootstrap__grub_cmdline_linux: "{{ bootstrap_grub_cmdline_linux | default('') }}"

bootstrap__grub_update_command: /usr/sbin/update-grub
