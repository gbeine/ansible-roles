---

bootstrap__fail2ban_ignoreip_default:
- 127.0.0.1/8
- ::1

bootstrap__fail2ban_ignoreip: "{{ bootstrap_fail2ban_ignoreip | default(bootstrap__fail2ban_ignoreip_default) }}"
bootstrap__fail2ban_bantime: "{{ bootstrap_fail2ban_bantime | default('1d') }}"
bootstrap__fail2ban_findtime: "{{ bootstrap_fail2ban_findtime | default('10m') }}"
bootstrap__fail2ban_maxretry: "{{ bootstrap_fail2ban_maxretry | default('5') }}"
