---
#
# Do not edit this file.
# Replace variables using substitutions described
# https://ansible-roles.org/roles/bootstrap
#

bootstrap__pre_setup_scripts: "{{ pre_setup_scripts | default([]) }}"

bootstrap_accounts__enable: "{{ bootstrap_accounts_enable | default(false) }}"
bootstrap_dotfiles__enable: "{{ bootstrap_dotfiles_enable | default(false) }}"
bootstrap_hosts__enable: "{{ bootstrap_hosts_enable | default(false) }}"
bootstrap_networking__enable: "{{ bootstrap_networking_enable | default(false) }}"
bootstrap_logrotate__enable: "{{ bootstrap_logrotate_enable | default(false) }}"
bootstrap_packages__enable: "{{ bootstrap_packages_enable | default(false) }}"
bootstrap_podman__enable: "{{ bootstrap_podman_enable | default(false) }}"
bootstrap_raspberrypi__enable: "{{ bootstrap_raspberrypi_enable | default(false) }}"
bootstrap_repositories__enable: "{{ bootstrap_repositories_enable | default(false) }}"
bootstrap_storage__enable: "{{ bootstrap_storage_enable | default(false) }}"
bootstrap_sudo__enable: "{{ bootstrap_sudo_enable | default(false) }}"
bootstrap_timezone__enable: "{{ bootstrap_timezone_enable | default(false) }}"
