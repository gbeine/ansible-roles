---
# tasks file for roles/bootstrap

- name: Add storage
  tags: [ bootstrap, mount, storage ]
  ansible.posix.mount:
    src: "{{ item.disk }}"
    path: "{{ item.path }}"
    opts: "{{ item.options }}"
    state: mounted
    fstype: "{{ item.fs }}"
  with_items: "{{ bootstrap__storage }}"
