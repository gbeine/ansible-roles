---
# tasks file for roles/bootstrap

- name: Enable and disable services
  tags: [ 'configuration' ]
  ansible.builtin.systemd:
    name: "{{ item.name }}.service"
    enabled: "{{ item.enabled | ternary('yes', 'no') }}"
  with_items: "{{ bootstrap__systemd_services }}"
