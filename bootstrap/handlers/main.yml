---
# handlers file for roles/bootstrap

- name: Generate locales
  when: not ansible_check_mode
  ansible.builtin.command: "{{ bootstrap__locales_gen_command }}"

- name: Update grub
  when: not ansible_check_mode
  ansible.builtin.command: "{{ bootstrap__grub_update_command }}"
