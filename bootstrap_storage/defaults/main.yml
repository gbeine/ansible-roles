---
#
# Do not edit this file.
# Replace variables using substitutions described
# https://ansible-roles.org/roles/bootstrap-storage
#

bootstrap_storage__devices: "{{ storage | default([]) }}"
bootstrap_storage__mount: "{{ mount | default([]) }}"
