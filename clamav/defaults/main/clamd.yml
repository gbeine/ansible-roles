---

clamav__clamd_enbale: "{{ clamav_clamd_enable | default(true) }}"

clamav__clamd_local_socket: "{{ clamav_clamd_local_socket | default('/var/run/clamav/clamd.ctl') }}"
clamav__clamd_fix_stale_socket: "{{ clamav_clamd_fix_stale_socket | default(true) }}"
clamav__clamd_local_socket_group: "{{ clamav_clamd_local_socket_group | default('clamav') }}"
clamav__clamd_local_socket_mode: "{{ clamav_clamd_local_socket_mode | default(666) }}"
clamav__clamd_user: "{{ clamav_clamd_user | default('clamav') }}"
clamav__clamd_scan_mail: "{{ clamav_clamd_scan_mail | default(true) }}"
clamav__clamd_scan_archive: "{{ clamav_clamd_scan_archive | default(true) }}"
clamav__clamd_archive_block_encrypted: "{{ clamav_clamd_archive_block_encrypted | default(false) }}"
clamav__clamd_max_directory_recursion: "{{ clamav_clamd_max_directory_recursion | default(15) }}"
clamav__clamd_follow_directory_symlinks: "{{ clamav_clamd_follow_directory_symlinks | default(false) }}"
clamav__clamd_follow_file_symlinks: "{{ clamav_clamd_follow_file_symlinks | default(false) }}"
clamav__clamd_read_timeout: "{{ clamav_clamd_read_timeout | default(180) }}"
clamav__clamd_max_threads: "{{ clamav_clamd_max_threads | default(12) }}"
clamav__clamd_max_connection_queue_length: "{{ clamav_clamd_max_connection_queue_length | default(15) }}"
clamav__clamd_log_syslog: "{{ clamav_clamd_log_syslog | default(false) }}"
clamav__clamd_log_rotate: "{{ clamav_clamd_log_rotate | default(true) }}"
clamav__clamd_log_facility: "{{ clamav_clamd_log_facility | default('LOG_LOCAL6') }}"
clamav__clamd_log_clean: "{{ clamav_clamd_log_clean | default(false) }}"
clamav__clamd_log_verbose: "{{ clamav_clamd_log_verbose | default(false) }}"
clamav__clamd_prelude_enable: "{{ clamav_clamd_prelude_enable | default('no') }}"
clamav__clamd_prelude_analyzer_name: "{{ clamav_clamd_prelude_analyzer_name | default('ClamAV') }}"
clamav__clamd_database_directory: "{{ clamav_clamd_database_directory | default('/var/lib/clamav') }}"
clamav__clamd_official_database_only: "{{ clamav_clamd_official_database_only | default(false) }}"
clamav__clamd_self_check: "{{ clamav_clamd_self_check | default(3600) }}"
clamav__clamd_foreground: "{{ clamav_clamd_foreground | default(false) }}"
clamav__clamd_debug: "{{ clamav_clamd_debug | default(false) }}"
clamav__clamd_scan_pe: "{{ clamav_clamd_scan_pe | default(true) }}"
clamav__clamd_max_embedded_pe: "{{ clamav_clamd_max_embedded_pe | default('10M') }}"
clamav__clamd_scan_ole2: "{{ clamav_clamd_scan_ole2 | default(true) }}"
clamav__clamd_scan_pdf: "{{ clamav_clamd_scan_pdf | default(true) }}"
clamav__clamd_scan_html: "{{ clamav_clamd_scan_html | default(true) }}"
clamav__clamd_max_html_normalize: "{{ clamav_clamd_max_html_normalize | default('10M') }}"
clamav__clamd_max_html_no_tags: "{{ clamav_clamd_max_html_no_tags | default('2M') }}"
clamav__clamd_max_script_normalize: "{{ clamav_clamd_max_script_normalize | default('5M') }}"
clamav__clamd_max_zip_type_rcg: "{{ clamav_clamd_max_zip_type_rcg | default('1M') }}"
clamav__clamd_scan_swf: "{{ clamav_clamd_scan_swf | default(true) }}"
clamav__clamd_exit_on_oom: "{{ clamav_clamd_exit_on_oom | default(false) }}"
clamav__clamd_leave_temporary_files: "{{ clamav_clamd_leave_temporary_files | default(false) }}"
clamav__clamd_algorithmic_detection: "{{ clamav_clamd_algorithmic_detection | default(true) }}"
clamav__clamd_scan_elf: "{{ clamav_clamd_scan_elf | default(true) }}"
clamav__clamd_idle_timeout: "{{ clamav_clamd_idle_timeout | default(30) }}"
clamav__clamd_cross_filesystems: "{{ clamav_clamd_cross_filesystems | default(true) }}"
clamav__clamd_phishing_signatures: "{{ clamav_clamd_phishing_signatures | default(true) }}"
clamav__clamd_phishing_scan_urls: "{{ clamav_clamd_phishing_scan_urls | default(true) }}"
clamav__clamd_phishing_always_block_ssl_mismatch: "{{ clamav_clamd_phishing_always_block_ssl_mismatch | default(false) }}"
clamav__clamd_phishing_always_block_cloak: "{{ clamav_clamd_phishing_always_block_cloak | default(false) }}"
clamav__clamd_partition_intersection: "{{ clamav_clamd_partition_intersection | default(false) }}"
clamav__clamd_detect_pua: "{{ clamav_clamd_detect_pua | default(false) }}"
clamav__clamd_scan_partial_messages: "{{ clamav_clamd_scan_partial_messages | default(false) }}"
clamav__clamd_heuristic_scan_precedence: "{{ clamav_clamd_heuristic_scan_precedence | default(false) }}"
clamav__clamd_structured_data_detection: "{{ clamav_clamd_structured_data_detection | default(false) }}"
clamav__clamd_command_read_timeout: "{{ clamav_clamd_command_read_timeout | default(30) }}"
clamav__clamd_send_buf_timeout: "{{ clamav_clamd_send_buf_timeout | default(200) }}"
clamav__clamd_max_queue: "{{ clamav_clamd_max_queue | default(100) }}"
clamav__clamd_extended_detection_info: "{{ clamav_clamd_extended_detection_info | default(true) }}"
clamav__clamd_ole2_block_macros: "{{ clamav_clamd_ole2_block_macros | default(false) }}"
clamav__clamd_allow_all_match_scan: "{{ clamav_clamd_allow_all_match_scan | default(true) }}"
clamav__clamd_force_to_disk: "{{ clamav_clamd_force_to_disk | default(false) }}"
clamav__clamd_disable_cert_check: "{{ clamav_clamd_disable_cert_check | default(false) }}"
clamav__clamd_disable_cache: "{{ clamav_clamd_disable_cache | default(false) }}"
clamav__clamd_max_scan_time: "{{ clamav_clamd_max_scan_time | default(120000) }}"
clamav__clamd_max_scan_size: "{{ clamav_clamd_max_scan_size | default('100M') }}"
clamav__clamd_max_file_size: "{{ clamav_clamd_max_file_size | default('25M') }}"
clamav__clamd_max_recursion: "{{ clamav_clamd_max_recursion | default(16) }}"
clamav__clamd_max_files: "{{ clamav_clamd_max_files | default(10000) }}"
clamav__clamd_max_partitions: "{{ clamav_clamd_max_partitions | default(50) }}"
clamav__clamd_max_icons_pe: "{{ clamav_clamd_max_icons_pe | default(100) }}"
clamav__clamd_pcre_match_limit: "{{ clamav_clamd_pcre_match_limit | default(10000) }}"
clamav__clamd_pcre_rec_match_limit: "{{ clamav_clamd_pcre_rec_match_limit | default(5000) }}"
clamav__clamd_pcre_max_file_size: "{{ clamav_clamd_pcre_max_file_size | default('25M') }}"
clamav__clamd_scan_xmldocs: "{{ clamav_clamd_scan_xmldocs | default(true) }}"
clamav__clamd_scan_hwp3: "{{ clamav_clamd_scan_hwp3 | default(true) }}"
clamav__clamd_max_rec_hwp3: "{{ clamav_clamd_max_rec_hwp3 | default(16) }}"
clamav__clamd_stream_max_length: "{{ clamav_clamd_stream_max_length | default('25M') }}"
clamav__clamd_log_file: "{{ clamav_clamd_log_file | default('/var/log/clamav/clamav.log') }}"
clamav__clamd_log_time: "{{ clamav_clamd_log_time | default(true) }}"
clamav__clamd_log_file_unlock: "{{ clamav_clamd_log_file_unlock | default(false) }}"
clamav__clamd_log_file_max_size: "{{ clamav_clamd_log_file_max_size | default(0) }}"
clamav__clamd_bytecode: "{{ clamav_clamd_bytecode | default(true) }}"
clamav__clamd_bytecode_security: "{{ clamav_clamd_bytecode_security | default('TrustSigned') }}"
clamav__clamd_bytecode_timeout: "{{ clamav_clamd_bytecode_timeout | default(60000) }}"
clamav__clamd_on_access_max_file_size: "{{ clamav_clamd_on_access_max_file_size | default('5M') }}"
