---
#
# Do not edit this file.
# Replace variables using substitutions described
# https://ansible-roles.org/roles/bootstrap-networking
#

bootstrap_networking__interfaces: "{{ bootstrap_networking_interfaces | default([]) }}"

bootstrap_networking__etc_dir: '/etc/network'
bootstrap_networking__interfaces_dir: "{{ bootstrap_networking__etc_dir }}/interfaces.d"
bootstrap_networking__interfaces_file: "{{ bootstrap_networking__etc_dir }}/interfaces"
