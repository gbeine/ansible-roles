---

- name: 'Restart Icinga'
  ansible.builtin.systemd_service:
    daemon_reload: "{{ monitoring_icinga_master__cert_source_dir | length > 0 }}"
    name: "{{ monitoring_icinga_master__container.name }}"
    state: 'restarted'
  when: 'not ansible_check_mode'
