---
# meta file for roles/mirror

dependencies:

- name: Generate logrotate files
  tags: [ 'mirror', 'logrotate', 'configuration' ]
  when: mirror__enable
  role: arillso.logrotate
  logrotate_global_config: false
  logrotate_applications: "{{ mirror__logrotate_files }}"
