---
# meta file for roles/airprint

dependencies:
- name: Setup cups
  role: cups
  vars:
    cups_enable: true
  tags:
  - airprint
  - cups
  when: airprint__enable

- name: Setup avahi
  role: avahi
  vars:
    avahi_enable: true
  tags:
  - airprint
  - avahi
  when: airprint__enable
