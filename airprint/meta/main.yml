---
# meta file for roles/airprint

dependencies:
- name: Setup cups
  tags: [ 'airprint', 'cups' ]
  when: airprint__enable
  role: cups
  vars:
    cups_enable: true

- name: Setup avahi
  tags: [ 'airprint', 'avahi' ]
  when: airprint__enable
  role: avahi
  vars:
    avahi_enable: true
