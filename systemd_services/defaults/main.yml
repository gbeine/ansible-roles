---

#
# Substitutes - do not change
#

systemd_services__services: "{{ systemd_services | default([]) }}"

#
# Defaults - do not change
#

systemd_services__etc_dir: /etc/systemd
systemd_services__system_dir: "{{ systemd_services__etc_dir }}/system"
