---
#
# Do not edit this file.
# Replace variables using substitutions described
# https://ansible-roles.org/roles/backup-influxdb-core
#

backup_influxdb_core__backup_repo: "{{ backup_influxdb_core_backup_repo | default('/var/backups/influxdb-core') }}"
backup_influxdb_core__data_dir: "{{ backup_influxdb_core_data_dir | default('/var/lib/influxdb-core') }}"
backup_influxdb_core__keep_days: "{{ backup_influxdb_core_keep_days | default('14') }}"
backup_influxdb_core__container: "{{ backup_influxdb_core_container | default('influxdb-core') }}"

#
# Defaults - do not change
#

backup_influxdb_core__files:
  - '910.backup.influxdb-core'
