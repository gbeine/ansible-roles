---

- name: 'Install PowerDNS Authoritative Server'
  tags: [ 'dns_powerdns' ]
  block:

    - name: 'Install PowerDNS Authoritative Server using podman'
      ansible.builtin.include_tasks:
        file: 'podman.yml'
      when: 'dns_powerdns__use_podman'
