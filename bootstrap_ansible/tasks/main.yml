---

- name: Setup ansible
  tags: [ 'bootstrap_ansible'  ]
  block:

    - name: Create ansible status directory
      ansible.builtin.file:
        path: /etc/ansible
        state: directory

    - name: Show facts
      ansible.builtin.debug:
        msg: "{{ ansible_facts }}"
      when: ansible_check_mode
