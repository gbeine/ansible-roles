---

supervisor__enable: "{{ supervisor_enable | default(false) }}"

supervisor__services: "{{ supervisor_services | default([]) }}"
supervisor__directories: "{{ supervisor_directories | default([]) }}"

supervisor__service_default:
  autostart: false
  autorestart: false
  environment: {}
