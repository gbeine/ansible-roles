---
# tasks file for roles/icinga

- name: Install and configure Icinga
  tags: [ 'icinga' ]
  when: icinga__enable
  include_tasks: icinga.yml

- name: Install and configure Icingaweb
  tags: [ 'icinga' ]
  when: icingaweb__enable
  include_tasks: icingaweb.yml
