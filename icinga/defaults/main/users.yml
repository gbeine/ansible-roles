---

icinga__conf_users_default:
  icingaadmin:
    class: User
    imports: [ 'generic-user' ]
    config:
      display_name: Icinga 2 Admin
      groups: [ 'icingaadmins' ]
      email: root@localhost

  icingaadmins:
    class: UserGroup
    config:
      display_name: Icinga 2 Admin Group
