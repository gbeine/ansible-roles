#
# {{ ansible_managed }}
#
remote-control:
  control-enable: {{ dns_unbound__control_enable | ternary('yes', 'no') }}
  # by default the control interface is is 127.0.0.1 and ::1 and port 8953
  # it is possible to use a unix socket too
  control-interface: /run/unbound.ctl
