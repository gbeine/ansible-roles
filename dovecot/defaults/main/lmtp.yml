---

dovecot__lmtp_proxy: "{{ dovecot_lmtp_proxy | default(false) }}"
dovecot__lmtp_save_to_detail_mailbox: "{{ dovecot_lmtp_save_to_detail_mailbox | default(false) }}"
dovecot__lmtp_rcpt_check_quota: "{{ dovecot_lmtp_rcpt_check_quota | default(false) }}"
dovecot__lmtp_add_received_header: "{{ dovecot_lmtp_add_received_header | default(true) }}"
dovecot__lmtp_hdr_delivery_address: "{{ dovecot_lmtp_hdr_delivery_address | default('final') }}"
dovecot__lmtp_client_workarounds: "{{ dovecot_lmtp_client_workarounds | default([]) }}"
dovecot__lmtp_mail_plugins: "{{ dovecot_lmtp_mail_plugins | default(['$mail_plugins']) }}"
