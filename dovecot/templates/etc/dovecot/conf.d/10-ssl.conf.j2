###
### {{ ansible_managed }}
###

##
## SSL settings
##

# SSL/TLS support: yes, no, required. <doc/wiki/SSL.txt>
ssl = {{ dovecot__ssl | ternary('yes', 'no') }}

# PEM encoded X.509 SSL/TLS certificate and private key. They're opened before
# dropping root privileges, so keep the key file unreadable by anyone but
# root. Included doc/mkcert.sh can be used to easily generate self-signed
# certificate, just make sure to update the domains in dovecot-openssl.cnf
{{ '#' if dovecot__ssl_cert | length == 0 }}ssl_cert = <{{ dovecot__ssl_cert }}
{{ '#' if dovecot__ssl_key | length == 0 }}ssl_key = <{{ dovecot__ssl_key }}

# If key file is password protected, give the password here. Alternatively
# give it when starting dovecot with -p parameter. Since this file is often
# world-readable, you may want to place this setting instead to a different
# root owned 0600 file by using ssl_key_password = <path.
{{ '#' if dovecot__ssl_key_password | length == 0 }}ssl_key_password = {{ '<' + dovecot__ssl_key_password }}

# PEM encoded trusted certificate authority. Set this only if you intend to use
# ssl_verify_client_cert=yes. The file should contain the CA certificate(s)
# followed by the matching CRL(s). (e.g. ssl_ca = </etc/ssl/certs/ca.pem)
{{ '#' if dovecot__ssl_ca | length == 0 }}ssl_ca = {{ '<' + dovecot__ssl_ca }}

# Require that CRL check succeeds for client certificates.
ssl_require_crl = {{ dovecot__ssl_require_crl | ternary('yes', 'no') }}

# Directory and/or file for trusted SSL CA certificates. These are used only
# when Dovecot needs to act as an SSL client (e.g. imapc backend or
# submission service). The directory is usually /etc/ssl/certs in
# Debian-based systems and the file is /etc/pki/tls/cert.pem in
# RedHat-based systems.
{{ '#' if dovecot__ssl_client_ca_dir | length == 0 }}ssl_client_ca_dir = {{ dovecot__ssl_client_ca_dir }}
{{ '#' if dovecot__ssl_client_ca_file | length == 0 }}ssl_client_ca_file = {{ dovecot__ssl_client_ca_file }}

# Require valid cert when connecting to a remote server
ssl_client_require_valid_cert = {{ dovecot__ssl_client_require_valid_cert | ternary('yes', 'no') }}

# Request client to send a certificate. If you also want to require it, set
# auth_ssl_require_client_cert=yes in auth section.
ssl_verify_client_cert = {{ dovecot__ssl_verify_client_cert | ternary('yes', 'no') }}

# Which field from certificate to use for username. commonName and
# x500UniqueIdentifier are the usual choices. You'll also need to set
# auth_ssl_username_from_cert=yes.
{{ '#' if dovecot__ssl_cert_username_field | length == 0 }}ssl_cert_username_field = {{ dovecot__ssl_cert_username_field }}

# SSL DH parameters
# Generate new params with `openssl dhparam -out /etc/dovecot/dh.pem 4096`
# Or migrate from old ssl-parameters.dat file with the command dovecot
# gives on startup when ssl_dh is unset.
{{ '#' if dovecot__ssl_dh | length == 0 }}ssl_dh = <{{ dovecot__ssl_dh }}

# Minimum SSL protocol version to use. Potentially recognized values are SSLv3,
# TLSv1, TLSv1.1, TLSv1.2 and TLSv1.3, depending on the OpenSSL version used.
#
# Dovecot also recognizes values ANY and LATEST. ANY matches with any protocol
# version, and LATEST matches with the latest version supported by library.
{{ '#' if dovecot__ssl_min_protocol | length == 0 }}ssl_min_protocol = {{ dovecot__ssl_min_protocol }}

# SSL ciphers to use, the default is:
ssl_cipher_list = {{ dovecot__ssl_cipher_list }}
# To disable non-EC DH, use:
#ssl_cipher_list = ALL:!DH:!kRSA:!SRP:!kDHd:!DSS:!aNULL:!eNULL:!EXPORT:!DES:!3DES:!MD5:!PSK:!RC4:!ADH:!LOW@STRENGTH

# Colon separated list of elliptic curves to use. Empty value (the default)
# means use the defaults from the SSL library. P-521:P-384:P-256 would be an
# example of a valid value.
{{ '#' if dovecot__ssl_curve_list is defined }}ssl_curve_list = {{ dovecot__ssl_curve_list }}

# Prefer the server's order of ciphers over client's.
ssl_prefer_server_ciphers = {{ dovecot__ssl_prefer_server_ciphers | ternary('yes', 'no') }}

# SSL crypto device to use, for valid values run "openssl engine"
{{ '#' if dovecot__ssl_crypto_device is defined }}ssl_crypto_device = {{ dovecot__ssl_crypto_device }}

# SSL extra options. Currently supported options are:
#   compression - Enable compression.
#   no_ticket - Disable SSL session tickets.
{{ '#' if dovecot__ssl_options is defined }}ssl_options = {{ dovecot__ssl_options }}
