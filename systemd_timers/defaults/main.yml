---

#
# Substitutes - do not change
#

systemd_timers__timers: "{{ systemd_timers | default([]) }}"

#
# Defaults - do not change
#

systemd_timers__etc_dir: /etc/systemd
systemd_timers__timer_dir: "{{ systemd_timers__etc_dir }}/system"
