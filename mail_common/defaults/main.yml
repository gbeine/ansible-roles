---

#
# Substitutes - do not change
#

mail_common__root_forward: "{{ mail_root_forward | default('') }}"
mail_common__domain: "{{ mail_domain | default('localdomain') }}"

mail_common__from: "{{ mail_from | default('') }}"

mail_common__smtp_host: "{{ mail_smtp_host | default('localhost') }}"
mail_common__smtp_port: "{{ mail_smtp_port | default(25) }}"
mail_common__smtp_user: "{{ mail_smtp_user | default('') }}"
mail_common__smtp_password: "{{ mail_smtp_password | default('') }}"
