---

#
# Substitutes - do not change
#

mail_common__root_forward: "{{ mail_root_forward | default('') }}"
mail_common__domain: "{{ mail_domain | default('localdomain') }}"
mail_common__smtp_server: "{{ mail_smtp_server | default('localhost') }}"
mail_common__smtp_server_port: "{{ mail_smtp_server_port | default(25) }}"
