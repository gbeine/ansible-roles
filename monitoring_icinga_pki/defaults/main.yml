---

#
# Substitutes - do not change
#

#
# Defaults - do not change
#

monitoring_icinga_pki__user: "{{ monitoring_icinga_common__user }}"
monitoring_icinga_pki__group: "{{ monitoring_icinga_common__group }}"

monitoring_icinga_pki__crt_dir: "{{ monitoring_icinga_common__crt_dir }}"

monitoring_icinga_pki__certificate_files: "{{ monitoring_icinga_common__certificate_files }}"
