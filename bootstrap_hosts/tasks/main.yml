---

- name: Create /etc/hosts
  tags: [ 'bootstrap_hosts'  ]
  block:

    - name: Generate /etc/hosts
      ansible.builtin.template:
        src: hosts.j2
        dest: /etc/hosts
        owner: root
        group: root
        mode: '644'
