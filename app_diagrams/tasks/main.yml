---

- name: Deploy diagrams.net container
  tags: [ 'app_diagrams' ]
  block:

    - name: Deploy container via podman
      ansible.builtin.import_role:
        name: podman
      vars:
        pod_containers:
          - "{{ app_diagrams__container }}"
