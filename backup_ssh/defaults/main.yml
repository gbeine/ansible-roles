---

#
# Substitutes - do not change
#

backup_ssh__key_size: "{{ backup_ssh_key_size | default(4096) }}"
backup_ssh__key_name: "{{ backup_ssh_key_name | default('backup') }}"
backup_ssh__key_path: "/tmp/{{ backup_ssh__key_name }}"
backup_ssh__comment: "{{ backup_ssh_comment | default('backup over SSH') }}"
