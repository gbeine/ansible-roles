---
#
# Do not edit this file.
# Replace variables using substitutions described
# https://ansible-roles.org/roles/secyrity-ssh
#

security_ssh__sshd_listen: "{{ security_sshd_listen | default(security_ssh__sshd_listen_default) }}"

security_ssh__etc_dir: '/etc/ssh'
security_ssh__ssh_config_dir: "{{ security_ssh__etc_dir }}/ssh_config.d"
security_ssh__sshd_config_dir: "{{ security_ssh__etc_dir }}/sshd_config.d"

security_ssh__sshd_listen_default:
  - '0.0.0.0'
  - '::'
