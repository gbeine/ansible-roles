---

#
# Substitutes - do not change
#

backup_gitlab__container: "{{ backup_gitlab_container | default('') }}"
backup_gitlab__container_name: "{{ backup_gitlab_container_name | default('') }}"
backup_gitlab__options: "{{ backup_gitlab_options | default('') }}"

#
# Do not change from here
#

backup_gitlab__files:
  - 910.backup.gitlab
