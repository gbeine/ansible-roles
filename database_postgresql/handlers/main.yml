---

- name: 'Restart PostgreSQL container'
  ansible.builtin.systemd_service:
    name: "{{ database_postgresql__container.name }}"
    state: 'restarted'
  when: 'not ansible_check_mode and database_postgresql__use_podman'
